export enum AccessNames {
    TOTALS = 'Total by product',
    CLOSING = 'Total by payment types',
    OPEN_ORDERS = 'Open orders',
    UNPAID = 'Unpaid Orders',
    FOR_EMPLOYEE = 'For Employee',
} 

export enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
}

export enum SpecialPayments {
    UNPAID = 'Por pagar',
    FOREMPLOYEE = 'Para empleado'
}
